[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-template"
version = "0.1.0"
description = "A minimal RAG framework."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]